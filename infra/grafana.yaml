version: '1.0'

#version: "3.9"
services:
  grafana:
    image: grafana/grafana-enterprise
    #container_name: grafana
    restart: unless-stopped
    ports:
      - '3000:3000'
    expose:
      - 3000